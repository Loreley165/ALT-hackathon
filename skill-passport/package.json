{
  "name": "alt-skill-passport",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/write-sw.js",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/learning-model.test.js && playwright test",
    "build:pages": "vite build --base=./ --outDir docs && node scripts/write-sw.js docs"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^6.0.0"
  }
}
